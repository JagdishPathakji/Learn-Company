-- What it is: Both are used to store numbers with decimals (like 4.5 or 19.99), but they calculate math fundamentally differently behind the scenes.

-- FLOAT (Approximate Precision) -> works when some error is acceptable
-- DECIMAL (Exact Precision) -> works when exact precise value is required to get stored